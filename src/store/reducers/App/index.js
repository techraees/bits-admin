const INITIAL_STATE = {
    user: {
        name: "Ghous Ahmed"
    }
};

const AppReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default AppReducer;

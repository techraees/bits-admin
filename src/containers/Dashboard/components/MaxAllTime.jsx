import React from 'react'

const MaxAllTime = ({
  dateFormat,
  setDateFormat,
  setFeatureName,
  featureName,
  dataValue,
  isLoading
}) => {
  return (
    <div style={{color:"white"}}>{dataValue}</div>

  )
}

export default MaxAllTime
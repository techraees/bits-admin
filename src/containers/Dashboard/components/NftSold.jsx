import React from 'react'

const NftSold = ({
  dateFormat,
  setDateFormat,
  setFeatureName,
  featureName,
  dataValue,
  isLoading,
}) => {
  return (
    <div style={{ color: "white" }}>{dataValue}</div>

  )
}

export default NftSold